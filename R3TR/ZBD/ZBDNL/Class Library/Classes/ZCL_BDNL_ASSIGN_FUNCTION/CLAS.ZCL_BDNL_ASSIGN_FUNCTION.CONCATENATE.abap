method concatenate.

  concatenate i01 i02 into e.

  if i03 is supplied.
    concatenate e i03 into e.
  endif.

  if i04 is supplied.
    concatenate e i04 into e.
  endif.

  if i05 is supplied.
    concatenate e i05 into e.
  endif.

endmethod.
